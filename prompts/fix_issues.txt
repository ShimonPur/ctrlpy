Bug Fix and Quality Improvement: Fix KaTeX, Nyquist Infinite Arc, Custom Exceptions, and Update Tutorial Notebook.

1. Fix LaTeX string formatting in `src/ctrlpy/models/state_space.py`:
   - Inspect `StateSpace._repr_latex_` and remove raw `\n` literals.
   - Replace line breaks in LaTeX equations with proper LaTeX newline markers `\\\\`.
   - Ensure the return string is wrapped cleanly in `$$ ... $$` and uses raw strings (`r"..."`).

2. Fix Nyquist plot algorithm in `src/ctrlpy/freq_domain.py` and `src/ctrlpy/plotting.py`:
   - Handle poles on the imaginary axis (specifically at s=0, integrators) by implementing an indented Nyquist D-contour:
     - For s ≈ 0, evaluate along $s = \epsilon e^{j\theta}$ for $\theta \in [-\pi/2, \pi/2]$ to compute the circular arc connecting $\omega = 0^-$ and $\omega = 0^+$.
   - Bound plot limits (`xlim`, `ylim`) to auto-scale reasonably around the critical point $(-1, 0j)$ instead of stretching infinitely along asymptotes.

3. Implement Custom Domain Exceptions in `src/ctrlpy/exceptions.py`:
   - Define base exception `CtrlPyError(Exception)`.
   - Define `UnstableSystemError(CtrlPyError)` and `UnstableSystemWarning(UserWarning)`.
   - Define `DimensionMismatchError(CtrlPyError)`.
   - Update `TimeResponseData` properties (`overshoot`, `settling_time`, `steady_state_value`): if the system has poles in the Right Half Plane (Re(p) > 0), raise `UnstableSystemError` with a clear message instead of returning invalid metrics.
   - Export these exceptions in `src/ctrlpy/__init__.py`.

4. Update and Re-execute `notebooks/tutorial.ipynb`:
   - Update all cells in the tutorial notebook to use the fixed classes and functions.
   - Ensure the cell displaying `sys_ss` renders valid LaTeX without KaTeX errors.
   - Ensure the Nyquist plot cell shows the updated, accurate contour around $(-1, 0j)$.
   - Add a brief example cell demonstrating how `UnstableSystemError` is raised when attempting transient analysis on an unstable system (e.g., $G(s) = \frac{1}{s - 1}$).

5. Verification:
   - Run `uv run ruff format .` and `uv run ruff check .`
   - Run `uv run pytest` (100% pass rate).